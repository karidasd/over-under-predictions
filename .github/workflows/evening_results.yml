name: Evening Results

on:
  schedule:
    - cron: '22 23 * * *'   # 23:22 UTC every day
  workflow_dispatch:          # Allow manual trigger from GitHub UI

jobs:
  fetch_results:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests python-dotenv

    - name: Run Results Evaluation Script
      env:
        API_FOOTBALL_KEY: "38f1c8e59dd3c061104a4ffd98f0fe1e"
      run: |
        python scripts/fetch_results.py

    - name: Commit and Push Updated Results & History
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: "🤖 Automated nightly results & history update"
        file_pattern: "docs/data/*.json"
